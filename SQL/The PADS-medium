SELECT 
  NAME || '(' || SUBSTR(OCCUPATION,1,1) || ')' AS OCCUPATION_NAME
FROM OCCUPATIONS
ORDER BY NAME ASC;

SELECT 
  'There are a total of ' || COUNT(*) || ' ' || LOWER(OCCUPATION) || 's.' AS OCCUPATION_COUNT
FROM OCCUPATIONS
GROUP BY OCCUPATION
ORDER BY COUNT(*) ASC, OCCUPATION ASC;
